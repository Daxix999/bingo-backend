# ============================================================
# VARIABLES DE ENTORNO - EJEMPLO
# Archivo: backend/env.example
# Descripción: Plantilla de variables de entorno necesarias
#              Copia este archivo como .env y completa los valores
# ============================================================

# ------------------------------------------------------------
# ENTORNO DE EJECUCIÓN
# ------------------------------------------------------------
# development = desarrollo local
# production = producción (Render, etc.)
NODE_ENV=development

# ------------------------------------------------------------
# PUERTO DEL SERVIDOR
# ------------------------------------------------------------
# Puerto donde correrá el servidor
# En Render se asigna automáticamente, pero puedes definir uno
PORT=5000

# ------------------------------------------------------------
# CONFIGURACIÓN DE BASE DE DATOS MySQL
# ------------------------------------------------------------
# Host de la base de datos
# Local: localhost
# Railway: tu-proyecto.mysql.railway.app
# PlanetScale: tu-proyecto.psdb.cloud
DB_HOST=localhost

# Puerto de MySQL (generalmente 3306)
DB_PORT=3306

# Usuario de la base de datos
DB_USER=root

# Contraseña del usuario
DB_PASSWORD=

# Nombre de la base de datos
DB_NAME=bingo_generator

# ------------------------------------------------------------
# JWT (JSON Web Token) - Autenticación
# ------------------------------------------------------------
# Secreto para firmar los tokens JWT
# IMPORTANTE: Genera uno seguro y único
# Puedes usar: openssl rand -base64 32
JWT_SECRET=tu_secreto_super_seguro_aqui_cambiar_obligatorio

# Tiempo de expiración del token (ej: 7d = 7 días, 24h = 24 horas)
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------
# URL DEL FRONTEND
# ------------------------------------------------------------
# URL donde está desplegado el frontend
# Local: http://localhost:3000
# Netlify: https://tu-sitio.netlify.app
FRONTEND_URL=http://localhost:3000

# ------------------------------------------------------------
# CONFIGURACIÓN DE ARCHIVOS
# ------------------------------------------------------------
# Tamaño máximo de archivos subidos (en bytes)
# 10MB = 10 * 1024 * 1024
MAX_FILE_SIZE=10485760

# Carpeta donde se guardan las imágenes subidas
UPLOAD_DIR=./uploads

# Carpeta donde se guardan los PDFs generados
PDF_DIR=./pdfs

# ------------------------------------------------------------
# CONFIGURACIÓN DE PUPPETEER (Generación de PDFs)
# ------------------------------------------------------------
# En Render, Puppeteer necesita estas configuraciones
# para funcionar correctamente
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false
PUPPETEER_EXECUTABLE_PATH=

# ------------------------------------------------------------
# CONFIGURACIÓN DE CRÉDITOS
# ------------------------------------------------------------
# Costo en créditos para generar un PDF final
COSTO_PDF_FINAL=5

# Créditos iniciales que reciben los usuarios nuevos
CREDITOS_INICIALES=0

# ------------------------------------------------------------
# CONFIGURACIÓN DE ADMIN
# ------------------------------------------------------------
# Email del administrador principal
# Este usuario se crea automáticamente con el seed.sql
ADMIN_EMAIL=admin@bingo.com

# Contraseña inicial del admin (cambiar después del primer login)
ADMIN_PASSWORD=admin123

# ============================================================
# INSTRUCCIONES:
# ============================================================
# 1. Copia este archivo: cp env.example .env
# 2. Completa todos los valores necesarios
# 3. NUNCA subas el archivo .env a Git (está en .gitignore)
# 4. En Render/Netlify, configura estas variables en el dashboard
# ============================================================

